Yarn Corporation Site
=============

To install:
```bash
$ git clone git@github.com:yarn-co/yarn-web.git
$ cd yarn-web
$ npm install
```
Start the server by running the command:
```bash
$ npm start
```
